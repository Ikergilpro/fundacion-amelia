# Logotipo oficial

El archivo actual es `logo.jpg`.

Si más adelante tienes una versión en PNG o SVG (fondo transparente), reemplázala aquí y actualiza `brand.logoSrc` en `src/data/site.ts`.

No recortes ni distorsiones el emblema. Conserva proporción y espacio alrededor.
